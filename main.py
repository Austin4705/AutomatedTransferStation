from flask import Flask, render_template, Response, jsonify
# from web_server import webServer as webServer
import asyncio
import socket_manager

socket = socket_manager.Socket()


print("hii")
while(True):
    pass