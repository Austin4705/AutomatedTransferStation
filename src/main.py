import threading
from dotenv import load_dotenv, find_dotenv
import os
import time




def orthogonally():
    print("Making the system orthogonal")
    # Attocube Align xy to 0, 0

    # Manually move the stage to about 10-15 degrees to the right of the center point where +- is the same as the attocube
    

if __name__ == '__main__':
    orthogonally()